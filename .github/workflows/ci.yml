name: CI
on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run ansible playbook
        run: |
          ansible-playbook --connection=local playbooks/app.yml
